<UserControl x:Class="DestinyMusicViewer.ProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DestinyMusicViewer"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">
    <Grid Width="1920" Height="1080" x:Name="Grid" Background="Transparent">
        <Rectangle IsHitTestVisible="True" Fill="#FF424242" Opacity="0.95"/>
        <StackPanel Orientation="Vertical" Height="105" Width="500" HorizontalAlignment="Center" VerticalAlignment="Center" Background="#FF2F2F2F">
            <ProgressBar x:Name="ProgressBar" Height="50" Value="50" Background="#FF2F2F2F" Foreground="#FFB74DFC"></ProgressBar>
            <Viewbox Height="50" Width="450">
                <TextBlock x:Name="ProgressText" Text="Loading" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Foreground="#FFDDDDDD"></TextBlock>
            </Viewbox>
        </StackPanel>
    </Grid>
</UserControl>
